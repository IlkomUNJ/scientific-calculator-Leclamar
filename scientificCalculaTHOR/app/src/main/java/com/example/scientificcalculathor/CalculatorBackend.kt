package com.example.scientificcalculathor

enum class Operation (val symbol : String) {
    PLUS ("+"),
    MINUS ("-"),
    MULTIPLY ("x"),
    DIVIDE ("/"),
    REMAINDER ("%")
}

enum class Trigonometry(){
    SIN,
    COS,
    TAN,
    ARCSIN,
    ARCCOS,
    ARCTAN
}
